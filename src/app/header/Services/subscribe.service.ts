export class SubscribeService {
  onSubscribeClicked(type: string) {
    // Add User To Database
    // Send Email With Subscription Detail
    // Allow user to access the service
    alert("You Have been subcribed ! For :" + type);
  }
}
